<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>


    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.FreakyApp"
        tools:targetApi="31">
        <service android:name=".location.LocationService" android:foregroundServiceType="location"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FreakyApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pagine_punti.LocationAActivity" />
        <activity android:name=".pagine_punti.LocationBActivity" />
        <activity android:name=".pagine_punti.LocationCActivity" />
        <activity android:name=".pagine_punti.LocationDActivity" />
        <activity android:name=".pagine_punti.LocViaNasoliniActivity" />
        <activity android:name=".pagine_punti.LocKaefuActivity" />
        <activity android:name=".pagine_punti.LocSegreteriaActivity" />
        <activity android:name=".pagine_punti.LocMagnaActivity" />
        <activity android:name=".pagine_punti.LocPalazzinaDActivity" />
        <activity android:name=".pagine_punti.LocMivaActivity" />
        <activity android:name=".pagine_punti.LocCheopeActivity" />
        <activity android:name=".pagine_punti.LocUnieuroActivity" />
        <activity android:name=".pagine_punti.LocMcActivity" />
        <service
            android:name=".location.LocationService"
            android:foregroundServiceType="location" />
    </application>

</manifest>